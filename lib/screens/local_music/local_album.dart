import 'package:flutter/material.dart';

class LocalAlbum extends StatelessWidget {
  const LocalAlbum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
